# AI 对话回归排查与修复

## 目标与边界

追溯提交后验证真实模型和数据调用，补齐现有服务与聊天的连接；保留账户隔离、已锁定分析前提、真实数据来源和确定性计算边界。初始工作区干净，HEAD 为 079510b。

## 证据与设计

1. b9a406e 引入正文工具校验；当前“请用通俗的语言解释一下市盈率”被误拦截。之前直接展示模型正文不等于有真实数据支持。
2. 当前账户持久化 DeepSeek 配置可以回答“你好”。问财 COMPANY_DATA/MARKET_DATA 请求返回 AUTH_FAILED；扶摇报价成功，但 ROE、毛利率、资产负债率为空。
3. 聊天只暴露行情、基金披露、文档检索和两个始终 BLOCKED 的组合工具；问财六类结构化查询没有聊天入口。已锁定上下文未包含完整风险画像供计算使用。
4. 选择局部连接现有服务；不整体回滚，因为回滚会恢复模拟计算及无证据正文旁路。调仓仍要求明确目标权重及受信任刷新，不自行补造目标。

## 实施与验证

1. 回归测试先复现概念措辞误拦截及缺少工具入口。
2. 补充有限概念语法、问财结构化查询工具、服务端事实驱动的持仓体检，输出原始字段及真实失败原因。
3. 定向及全量回归；真实模型复测；问财有效凭据更新后再验证真实数据结果。
4. 更新 LOG/TODO，检查差异并提交任务变更，不推送。

## 验证结果

- 已完成措辞识别、六类真实结构化查询、逐请求可用性判断、锁定 LIVE 体检和原始字段展示。调仓保留结构化目标与受信任刷新前提。
- 九项实际探测为六项成功、行情/财务/行业三项 AUTH_FAILED（财务单项 HTTP 401）；已配置 Key 并非全面失效，外部权限待核实。
- 找到另一工作树尚未合入 main 的 `3d96231` 及后续提交；其持久化 `verified_skills` 元数据导致 main 加载配置失败，现兼容读取并完成实际导入验证。
- 隔离真实行情环境变量后全量 782 passed、9 skipped；最终兼容及对话定向 31 passed。真实模型概念、宏观与公告查询通过，隔离 HTTP/SSE 概念及宏观查询 HTTP 200、有正文及 DONE。浏览器登录态未验收，三项外部权限仍未完成。

## 第二轮：Key 输入与组合链路

用户要求修复 Key 输入/持久化，移植另一工作树相关组合修复，并实际对话验收。起点 7ade47c、工作区干净。

- 提交与代码证据：`360218a` 引入状态刷新时无条件清空 Key；`e2da1dd` 的 PUT 空 Key 语义会删除凭据；`04a0f18` 的前端刷新条件将扶摇报价与问财配置绑定。浏览器对密码输入的 DOM 读数不能作为真实长度证据，本轮改用受控保存后的服务端布尔比较验证输入链路。
- 方案比较：整体移植另一分支会覆盖本次主线新报告/OCR与前端；选择按文件/函数移植行情批量请求、真实行业解码及确认来源保留，单独修复 Key 表单与 API 删除语义。
- 已完成：空 Key 保留原凭据、显式 DELETE 恢复默认、草稿不受状态刷新覆盖、跨工作树统一默认存储路径；移植批量报价、限流重试、行业标准化及确认来源保留。股票真实价格完整而行业缺失时返回部分体检组合，不注册为可调仓的受信任刷新。
- 已完成：新增持仓明细行业确认入口，PATCH 只更新已保存股票行业并保留价格/数量；常规持仓替换保留已有行业确认。自然语言体检缺少锁定资料时提供确认按钮；行业未确认时明确快照计算边界。
- 浏览器实测：切换回复模式后的测试草稿成功到达后端；重新打开后空保存保留有效 Key 与模型；连接测试通过，重启后恢复。测试时曾意外保存测试草稿，已告知用户并取得明确同意，将本机已验证的加密模型配置用于当前账户，最终确认有效配置保留。
- 浏览器实测：概念问答返回正文；300750 查询返回真实报价，财务查询显示 AUTH_FAILED；快捷体检及锁定资料后的自然语言体检返回确定性结果；调仓入口列出五项行业缺失并链接至可操作的行业确认入口。未替用户猜测行业或生成交易。
- 验证：全量 800 passed、9 skipped；最终修复定向 50 passed；Node 表单竞争请求/失败保留回归及语法检查通过。修复版正式账户服务运行于 127.0.0.1:8878，原 8000 进程未替换。问财行情/财务/行业权限问题仍需外部处理。

## 第三轮：自动行业与问财额度

- 目标：撤回人工行业确认，自动取得真实行业；定位 AUTH_FAILED 的实际原因。
- 方案比较：继续只依赖问财需等待额度恢复；东方财富公司资料按证券代码查询可与问财额度解耦。选择后者作为行业自动源，原问财保留备用，不加入股票硬编码。push2 曾成功后连接中断，最终使用已实测五股成功的 datacenter 公司资料接口。
- 已完成：删除人工表单及 PATCH；自动分类、缓存及来源持久化；401 正文额度耗尽单独分类 QUOTA_EXHAUSTED，真实认证失败仍保留原分类。
- 实际验收：五项行业自动填充后，8878 登录页面调仓测算成功；集中度超限仍显示风险复核。真实模型财务对话准确显示额度耗尽，而非让用户更换 Key。
- 验证：全量 805 passed、9 skipped，最终接口定向 42 passed。财务查询仍需外部额度恢复，未虚构缺失数据。

## 第四轮：聊天结果、配置与报告交互

- 目标：移除回复模式和独立测试按钮，保存自动测试；自然语言个股研判复用下方卡片；发送旁常驻过程状态；修复报告冲突、问卷切页定位与程度选项。
- 路径比较：前端关键词分流容易丢失标的与上下文，选择模型工具结果驱动同一结果卡；整体重写研究服务超出当前展示问题，选择补接已有扶摇估值及财务端点，缺少历史分位时明确保留边界。
- 证据：宁德时代估值快照、2026 中报及财务指标真实请求成功；当前 get_quote 只调用行情和元信息。报告按快照编号生成 ID，自动行业更新保留编号会引发不可变内容冲突；改为内容身份。
- 验证计划：报告更新及重复读取回归、财务标的/报告期/空值与失败回归、配置自动测试状态回归、实际页面保存/研判/报告/问卷验证。保留现有账户、密钥、持仓数量和问卷评分。

- 第四轮结果：名称检索、最新估值及报告期财务接口、共用卡片、常驻进度、保存自动测试、报告内容编号、问卷程度选项及顶部定位均完成。实际页面验证包括自然语言“研判宁德时代”、保留 Key 保存后自动测试、正式报告重复生成及问卷切页。最终全量 810 passed、9 skipped，相关定向 93 passed，Node 配置状态回归通过。历史估值分位与完整个股配置适当性保留为服务缺口。
