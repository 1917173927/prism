# AI 对话回归排查与修复

## 目标与边界

追溯提交后验证真实模型和数据调用，补齐现有服务与聊天的连接；保留账户隔离、已锁定分析前提、真实数据来源和确定性计算边界。初始工作区干净，HEAD 为 079510b。

## 证据与设计

1. b9a406e 引入正文工具校验；当前“请用通俗的语言解释一下市盈率”被误拦截。之前直接展示模型正文不等于有真实数据支持。
2. 当前账户持久化 DeepSeek 配置可以回答“你好”。问财 COMPANY_DATA/MARKET_DATA 请求返回 AUTH_FAILED；扶摇报价成功，但 ROE、毛利率、资产负债率为空。
3. 聊天只暴露行情、基金披露、文档检索和两个始终 BLOCKED 的组合工具；问财六类结构化查询没有聊天入口。已锁定上下文未包含完整风险画像供计算使用。
4. 选择局部连接现有服务；不整体回滚，因为回滚会恢复模拟计算及无证据正文旁路。调仓仍要求明确目标权重及受信任刷新，不自行补造目标。

## 实施与验证

1. 回归测试先复现概念措辞误拦截及缺少工具入口。
2. 补充有限概念语法、问财结构化查询工具、服务端事实驱动的持仓体检，输出原始字段及真实失败原因。
3. 定向及全量回归；真实模型复测；问财有效凭据更新后再验证真实数据结果。
4. 更新 LOG/TODO，检查差异并提交任务变更，不推送。

## 验证结果

- 已完成措辞识别、六类真实结构化查询、逐请求可用性判断、锁定 LIVE 体检和原始字段展示。调仓保留结构化目标与受信任刷新前提。
- 九项实际探测为六项成功、行情/财务/行业三项 AUTH_FAILED（财务单项 HTTP 401）；已配置 Key 并非全面失效，外部权限待核实。
- 找到另一工作树尚未合入 main 的 `3d96231` 及后续提交；其持久化 `verified_skills` 元数据导致 main 加载配置失败，现兼容读取并完成实际导入验证。
- 隔离真实行情环境变量后全量 782 passed、9 skipped；最终兼容及对话定向 31 passed。真实模型概念、宏观与公告查询通过，隔离 HTTP/SSE 概念及宏观查询 HTTP 200、有正文及 DONE。浏览器登录态未验收，三项外部权限仍未完成。
