# 对话与报告运行验收

## 目标与约束
修复 8000 的正式报告 409 和普通对话误要求工具，必须实际页面验证。保留真实金融依据约束，不用未核验正文替代工具数据，不变更用户 Key 或持仓。

## 已确认原因
- 旧 reload 父进程退出，但 PID 46844 子进程保留 8000 监听句柄；先前只杀父进程无效。已停止核实的父子进程，以当前主工作区启动 PID 42704，报告页面恢复。
- 普通对话采用固定词表，未知表述默认要求工具；aca2a5b 放行所有正文破坏金融依据约束且测试矛盾。

## 方案
对比继续词表补丁、放行全部正文与结构化意图分流，选择分流：真实模型先通过专用工具判定是否依赖金融数据，普通对话不携带金融工具，数据请求强制工具调用。失败仍安全拒绝无依据数据。代价为一次额外意图请求。

## 验证
- [x] 复现原页面两个错误、重启真实服务后报告显示。
- [x] 分流、未调用工具、分类失败及真实金融工具结果回归；拒绝非布尔分类结果与未核验股价。
- [x] 实际页面验证原句、自我介绍追问、宁德时代研判、研判后的概念追问、报告重复生成、个人中心切换；最终代码重启后再次验证原句和报告。
- [x] 更新日志并准备任务提交，仅汇报实际通过项目。最终全量回归 827 passed、9 skipped、2 warnings；git diff --check 通过。

## 运行验收补充
- DeepSeek 实际返回 HTTP 400：Thinking mode does not support this tool_choice。仅为 api.deepseek.com 的强制工具请求关闭 thinking，真实分类及金融查询恢复。
- 报告页面显示五项持仓及报告 portfolio-report-2f1f482318c75cf71af275eba35fdb00，按编号读取持久化报告返回 200。撤销吞掉存储冲突后伪报成功的逻辑，真实冲突返回明确的 409 错误。
- 原先声称通过的测试与提交版本不一致，不能作为当前修复证据。本轮对最终代码重新执行全量回归；其中旧 UI 文案断言与现有 HTML 不一致，已校正断言，未更改页面文案。
