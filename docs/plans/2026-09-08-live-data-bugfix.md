# 真实数据接入后的用户流程缺陷修复

## 范围与约束

- 仅修复现有功能的可复现缺陷，不新增业务能力。
- 保留原有 README.md 与 docs/prd-integration-guide.md 未提交内容。
- 真实数据缺失、请求失败与合成示例保持明确区分；风险画像与金融计算由后端确定性服务裁决。
- 使用独立本地服务的内存数据库体验问卷、持仓、体检、研究、调仓和模式切换，避免污染当前用户服务状态。

## 排查与方案

| 环节 | 已验证事实 | 待验证问题 | 处理原则 |
| --- | --- | --- | --- |
| 基线 | 557 项测试通过；8000 端口 LIVE 扶摇行情成功 | 页面流程覆盖不足 | 增加针对根因的行为回归 |
| 首次使用 | 问卷未完成时首页持续计算中，体检报无后端结果 | 画像、持仓前置条件未正确呈现 | 显式引导既有问卷与持仓入口 |
| 数据消费 | 真实行情缺少财务指标 | 空值、资产类型、时间与模式切换的一致性 | 缺失不转零，不补造数据 |
| 后续操作 | 待体验 | 研究、体检、调仓及失败恢复 | 对原路径复测 |

方案比较：可以重构统一任务编排，也可以修复现有前置检查、数据映射与请求生命周期。选择后者，改动范围较小，保留现有接口与交互结构；仅在重复根因需要时复用已有辅助函数。

## 执行与验证

1. [x] 检查工作区、启动方式、数据源与测试基线。
2. [x] 浏览器体验现有用户流程，并以最小探针确认根因。
3. [x] 实施局部修复与回归测试。
4. [x] 重跑原浏览器路径、适用测试及静态检查。
5. [x] 更新 LOG/TODO，审查差异，仅提交本轮改动。

## 已验证结果

| 路径 | 根因 | 修复行为 | 验证 |
| --- | --- | --- | --- |
| 首次使用 | 前置依赖自动引入合成画像/持仓 | 仅使用用户确认输入，展示缺失项 | 浏览器与 JS 行为测试 |
| 股票与基金研究 | 共用股票接口，缺失财务转零，旧响应覆盖 | 按资产类型路由，保留空值，检查请求上下文 | 真实接口浏览器与回归 |
| 聊天 | 默认基金代码替代用户代码；可选字段直接索引 | 保留代码、来源、时间与缺失状态 | 510300/600519 浏览器与回归 |
| 持仓导入 | 静态价格覆盖现价；LIVE 附加合成 ETF 分布 | 按用户现价计算，缺失穿透保持未知 | 340000 元股票组合、80000 元基金组合 |
| 体检图表 | 总体超限被等同为某行业超限；固定科技暴露 | 分别展示 HHI 与行业裁决，读取实际体检值 | HHI 单独超限浏览器与回归 |
| 调仓与压力测试 | 空目标假成功、穿透目标作为交易资产、未分类暴露抛 500 | 明确拒绝原因与输入边界 | 浏览器与 API/JS 回归 |

全量 572 项测试通过；新增 15 项回归。Node 语法、Python 编译和差异检查通过。LIVE → MOCK → LIVE 浏览器切换已验证，旧体检及研究结果正确清除。仅覆盖本轮复现路径；外部数据缺口不以合成数据补齐。

## 用户原文追加复现：千位分隔数字

- [x] 原文复现：千位逗号导致数量、成本与现金截断，0 股使确认接口返回 422，页面显示解析出错。
- [x] 方案比较：逐处扩大数字正则，或在提取前统一规范化合法千位分组。采用后者，避免数量、价格与现金规则不一致，普通逗号保留。
- [x] 增加原文与无逗号对照、小数和多组千位回归；先失败后通过，相关 37 项测试通过。
- [x] 当前 8000 服务 LIVE 浏览器原文录入成功，5 项资产与 28000 元现金正确；记录并仅提交本轮修复。

## 导入取价超时追加处理

- [x] 确认单项 1.5 秒总截止时间与任意失败立即终止链路；当前上游探针成功，不能据此还原历史超时原因。
- [x] 比较全局延长超时与仅导入瞬时失败重试；选择后者，单次超时不变，每只最多两次，错误类型受限，不改变其他查询路径。
- [x] 故障注入验证恢复、重试上限和凭据错误不重试；相关 38 项测试通过，真实原文 LIVE 请求约 2.6 秒成功。

## 刷新丢失与完整用户路径追加复核

- [x] 确认文本持仓未保存、内存数据库会随重启丢失、页面缺少恢复路径。
- [x] 复用 SQLite 持久化设施，按 owner 和数据模式保存最后确认的持仓、数量、成本、现金；默认桌面入口使用 data/private/prism.sqlite3，测试工厂保留内存隔离。
- [x] 页面启动在数据模式及问卷加载后恢复持仓，重新计算体检；保存成功与体检失败分别反馈。
- [x] 浏览器验证问卷、原文导入、刷新、重启、体检、研究、调仓及截图路径；修复跨页面弹窗、模式标签、截图编号冲突。
- [x] 验证数据库重开、owner/mode 隔离、失败写入及过期恢复响应；全量 582 项通过，随后相关 28 项通过，记录见 LOG。

方案比较：浏览器缓存全部状态实现较快，但无法统一问卷、截图确认与服务端生命周期；选择现有 SQLite 存储，并只新增当前持仓记录和恢复接口，不缓存派生投资结论。

## 零交易调仓结果追加复核

- [x] 当前 8000 用户页面复现：茅台 52.19% → 50.00%，约 25 股减持量按现有 100 股单位向下取整为 0；全 HOLD 被标为 READY，主卡未呈现原因。
- [x] 体检现金 1.86% < 5.00%，优化目标现金仅 4.05%；原再平衡未接收体检现金约束。
- [x] 方案比较：自动扩大卖出量满足整手，或保留数量算法并返回约束未落实的复核结果。选择后者，避免擅自扩大交易规模；不将零交易当作风险已解除。
- [x] 后端记录取整归零原因，接收 minimum_cash_pct 并检查目标与扣费后现金；前端传递体检要求，显示复核状态和明细原因。旧 API 调用默认现金下限为 0，保留兼容性。
- [x] 浏览器原路径复核成功；全量 584 项通过，最终相关 28 项通过，记录见 LOG。目标优化器尚未实现现金下限修复，现以明确复核状态拒绝将该缺口视为达标。
